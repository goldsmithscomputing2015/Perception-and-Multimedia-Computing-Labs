// int radius = 175;
// int screenWidth = 400;
// int[] aspectRatio = {1,1};
// int elements = 1000;

void setup(){
	// size(screenWidth,screenWidth/aspectRatio[0]*aspectRatio[1]);
	size(400,400);
}

void draw(){
	// background(0,0,0);
	// translate(screen.width/2,screen.height/2);
	// for(int i=0;i<elements;i++){
	// 	strokeWeight(2);
	// 	point(cos((TWO_PI/elements)*i)*radius,sin((TWO_PI/elements)*i)*radius);
	// }
}